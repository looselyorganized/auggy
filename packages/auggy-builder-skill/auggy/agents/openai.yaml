interface:
  display_name: "Auggy Builder"
  short_description: "Build and explain Auggy app backends"
  default_prompt: "Use $auggy to set up or extend an Auggy agent-native app backend."

policy:
  allow_implicit_invocation: true
