node_modules
# The published runtime ships this complete tree. Keep every generated file
# visible to Git so the root provenance check can detect additions and drift.
!dist/
!dist/**
.vite
*.tsbuildinfo
